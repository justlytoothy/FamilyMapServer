package service;

import result.AllFamilyResult;

/**
 * Performs finding of all family members of current user and returns the results
 */
public class AllFamilyService {
    /**
     * Finds the person identified by provided auth token
     * associated with the logged-in user
     * @param authtoken the authtoken of the logged-in user
     * @return the result of searching for the family members of the logged-in user
     */
    public AllFamilyResult find (String authtoken) {
        return null;
    }

}
