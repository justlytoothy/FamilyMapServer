package service;
import result.FindEventResult;

/**
 * Service used to find events by their ID's and return results
 */
public class FindEventService {
    /**
     * Finds event associated with provided event ID
     * Then if event is associated with logged-in user returns it
     * @param eventID the ID of the desired event
     * @return the results of trying to get the event
     */
    public FindEventResult find(String eventID) {return null;}
}
