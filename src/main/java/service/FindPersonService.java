package service;

import result.FindPersonResult;

/**
 * Performs finding of one person operations and returns the results
 */
public class FindPersonService {
    /**
     * Finds the person identified by provided person ID and returns them only if they are
     * associated with the logged-in user
     * @param personID the username of the logged-in user
     * @return the result of searching for the provided person ID
     */
    public FindPersonResult find (String personID) {
        return null;
    }

}
