package service;

import request.RegisterRequest;
import result.RegisterResult;

/**
 * Links the register request and result together
 */
public class RegisterService {
  /**
   * This method takes in a request to register a new user, gets the result and then sends back to the handler
   * @param request the request sent to register a new user
   * @return
   */
  public RegisterResult register(RegisterRequest request) {
    return null;
  }
}
